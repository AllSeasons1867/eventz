<ul>
    <% @events.each do |event| %>
        <li><%= event.name %></li>
        <li><%= event.location %></li>
        <li><%= number_to_currency(event.price,precision:0) %></li>
    <% end %>
</ul>